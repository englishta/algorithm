#pragma region Macros
#include<bits/stdc++.h>
using namespace std;
using ll = long long;
#define rep(i,n) for(ll i=0; i<(n); i++)

int main(void){
    int n; cin >> n;
    ll ans = 1;
    for(int i=2; i<=n; i++){
        
    }
    return 0;
}
